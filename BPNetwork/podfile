# Uncomment the next line to define a global platform for your project
  platform :ios, '9.0'

target 'BPNetwork' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BPNetwork
  pod 'Alamofire', '~> 4.4'
  pod "PromiseKit", "~> 3.5"

  target 'BPNetworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BPNetworkUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
